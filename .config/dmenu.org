

#+begin_src h
/* See LICENSE file for copyright and license details. */
/* Default settings; can be overriden by command line. */

static int topbar = 1;                      /* -b  option; if 0, dmenu appears at bottom     */
static int centered = 0;                    /* -c option; centers dmenu on screen */
static int min_width = 900;                    /* minimum width when centered */
static unsigned int border_width = 3; /* border width in px */

/* -fn option overrides fonts[0]; default X11 font or font set */
static const char *fonts[] = {
	"SpaceMono Nerd Font Mono:size=12"
};

/* prompt and colors tuned to the site's look */
static const char *prompt = "▸ ";

static const char *colors[SchemeLast][2] = {
	[SchemeNorm] = {"#e6e6d8", "#ffd400"},
	[SchemeSel] = {"#ffd400", "#ffd400"},
	[SchemeOut] = {"#ffd400", "#f2c94c"},
};

/* roomy single-line height (requires lineheight patch) */
static unsigned int lineheight = 26;

/* alpha/transparency (requires alpha patch) */
static const unsigned int alphas[SchemeLast][2] = {
	/* fg alpha, bg alpha */
	[SchemeNorm] = {OPAQUE, 0xee},  /* slightly transparent bg */
	[SchemeSel] = {OPAQUE, OPAQUE}, /* solid selection */
	[SchemeOut] = {OPAQUE, OPAQUE},
};

/* new option added by padding/box patch */
static unsigned int horiz_padding = 12;  /* horizontal padding in px */
static unsigned int vert_padding  = 6;   /* vertical internal padding (if supported) */
#+end_src
